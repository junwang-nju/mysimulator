
#ifndef _Array_1D_Fill_H_
#define _Array_1D_Fill_H_

#include "array/1d/interface.h"
#include "array/1d/content/fill.h"

/*
namespace mysimulator {

  template <typename T1, typename T2>
  void fill(Array1D<T1>& v, const T2& d, const int& fst,
            const unsigned int& del, const unsigned int& n) {
    assert(IsValid(v)&&IsValid(d));
    fill(v.content,d,fst,del,n);
  }

  template <typename T1, typename T2>
  void fill(Array1D<T1>& v,const T2& d,const int& fst,const unsigned int& n) {
    assert(IsValid(v)&&IsValid(d));
    fill(v.content,fst,n);
  }

  template <typename T1, typename T2>
  void fill(Array1D<T1>& v,const T2& d,const unsigned int& n) {
    assert(IsValid(v)&&IsValid(d));
    fill(v.content,d,n);
  }

  template <typename T1, typename T2>
  void fill(Array1D<T1>& v, const T2& d) {
    assert(IsValid(v)&&IsValid(d));
    fill(v.content,d);
  }

}
*/

#endif

